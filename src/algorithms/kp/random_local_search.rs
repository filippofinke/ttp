use crate::models::{instance::Instance, solution::Solution};
use std::path::Path;

pub struct RandomLocalSearchKP;

impl RandomLocalSearchKP {
    pub fn solve(path: &Path, instance: &Instance) -> Solution {}
}
